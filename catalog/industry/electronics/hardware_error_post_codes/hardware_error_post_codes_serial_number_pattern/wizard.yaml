version: '2.0'
metadata:
  title: Hardware Error Post Codes Serial Number Pattern
  description: Regex avanzado para Hardware Error Post Codes Serial Number Pattern
    en entorno de home. Optimizado para precisión y análisis de grupos léxicos O(N).
  tags:
  - home
  - delcenjo
  - ai_generated
  - strict
  priority: 63
wizard:
  steps: []
logic:
  base_pattern: (?:.*?)(?P<generic_key>[A-Za-z0-9_-]+)\s*[:=]\s*(?P<generic_val>(?:\d+(?:\.\d+)?)|(?:[\"'].*?[\"']))(?:.*?)
corpus:
  matches:
  - value: Match_example_for_home
    label: Valid Case
  non_matches:
  - value: Inval1d~!
    reason: Falla validaciones métricas strictas
